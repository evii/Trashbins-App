<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorGreen500</item>
        <item name="colorPrimaryDark">@color/colorGreen800</item>
        <item name="colorAccent">@color/colorRed500</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>


    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>


    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="widget_text1">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">@dimen/widget_text</item>
        <item name="android:padding">@dimen/widget_text_padding</item>
    </style>

    <style name="widget_text2">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">@dimen/widget_text</item>
        <item name="android:paddingStart">@dimen/widget_text_padding</item>
        <item name="android:paddingLeft">@dimen/widget_text_padding</item>
        <item name="android:paddingEnd">@dimen/widget_text_padding</item>
        <item name="android:paddingRight">@dimen/widget_text_padding</item>
    </style>


</resources>
